#!/bin/sh
# Strip Cursor / AI co-author trailers injected by IDE or cloud agents.
tmp=$(mktemp)
sed -E '/^[Cc]o-authored-by:.*[Cc]ursor/d; /^[Cc]o-authored-by:.*cursoragent@cursor\.com/d' "$1" > "$tmp" && mv "$tmp" "$1"
