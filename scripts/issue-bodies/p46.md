# GitHub Task: Dispatch orders picking and loading

**Title**: [P46] Dispatch: Orders, picking, and loading

**Description**:
Implement dispatch and loading per PRD Phase 6. Create dispatch orders against customer/product, pick from finished goods stock, record loading, and post inventory OUT. Optional link to gate module for outbound truck tracking.

**PRD references**: Section 54 Dispatch and loading, Section 55 Outbound gate flow, Section 50 Inventory traceability

**Depends on**: P45 finished goods, P2 gate module (#gate)

**Task Pointers (Checklist)**:
- [ ] **Schema**: Add `DispatchOrder`, `DispatchOrderLine`, `DispatchLoad` models (customer, product, qty, status, truckRef optional)
- [ ] **Migration**: `prisma/migrations/*_dispatch`
- [ ] **API**: `GET/POST /api/dispatch/orders` - CRUD dispatch orders
- [ ] **API**: `POST /api/dispatch/orders/{id}/pick` - allocate FG lots to order lines
- [ ] **API**: `POST /api/dispatch/orders/{id}/load` - confirm loading, inventory OUT
- [ ] **UI**: `/dashboard/dispatch` - order list, create order, pick and load workflow
- [ ] **RBAC**: DISPATCH module canRead/canCreate/canUpdate
- [ ] **Sidebar**: Confirm Dispatch nav resolves (no 404)
- [ ] **Logging**: Audit order create, pick, load with full diff
- [ ] **OpenAPI**: Register dispatch routes
