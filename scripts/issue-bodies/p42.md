# GitHub Task: Quality Control scrap and waste tracking

**Title**: [P42] Quality Control: Scrap and waste tracking

**Description**:
Formalise scrap recording per PRD. Capture scrap from QC decisions and production runs with reason codes, phase, material type, and quantity. Post inventory OUT movements for scrapped material using the existing inventory transaction framework.

**PRD references**: Section 42 Scrap and waste, Section 50 Production to Inventory

**Depends on**: P36 inventory integration (#227), P39

**Task Pointers (Checklist)**:
- [ ] **Schema**: Add `ScrapRecord` model (sourceType, sourceId, phase, reasonCode, quantity, inventoryItemId, recordedById, recordedAt)
- [ ] **Schema**: Optional `ScrapReasonCode` master in Data Centre
- [ ] **Migration**: `prisma/migrations/*_scrap_tracking`
- [ ] **API**: `GET/POST /api/quality/scrap` - list and record scrap
- [ ] **Lib**: `postScrapInventoryMovement` using `inventory-tx` pattern (referenceType SCRAP)
- [ ] **UI**: Scrap log on `/dashboard/quality` with filters by phase, date, reason
- [ ] **UI**: Record scrap from inspection detail and from production run complete flows
- [ ] **Logging**: Audit all scrap records
